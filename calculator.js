// Data from previous file
const baseFreight = 10;
const weight = 1000;
const oda = 1000;

const totalFreight = totalFreight() // this fn returns Total Freight using the formula Total Freight = (Base Freight x Weight)
{
    return (baseFreight * weight);
}
